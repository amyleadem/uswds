

export default {
  title: "Tokens/Section",
  argTypes: {
    modifier: {
      options: [
        'usa-section', 
        'usa-section--dark', 
        'usa-section--light', 
      ],
      control: { type: 'select' },
    }
  }
}

// const Template = (args) => Component(args);

// export const Default = Template.bind({});
